trigger:
- main

variables:
  imageName: joke-app
  acrLoginServer: jokeapp.azurecr.io
  tag: $(Build.BuildId)

pool:
  name: Self-Hosted-Pool

stages:
# =============================
# STAGE 1: BUILD & PUSH IMAGE
# =============================
- stage: Build
  displayName: Build and Push Docker Image
  jobs:
  - job: BuildJob
    displayName: Docker Build Job
    steps:
    - task: Docker@2
      displayName: Build and Push Image to ACR
      inputs:
        containerRegistry: 'jokeapp-service-connection'
        repository: '$(imageName)'
        command: 'buildAndPush'
        Dockerfile: '**/Dockerfile'
        tags: |
          $(tag)          # ✅ PROD TAG ONLY

# =============================
# STAGE 2: DEPLOY TO AKS
# =============================
- stage: Deploy
  displayName: Deploy to AKS (Prod)
  dependsOn: Build
  condition: succeeded()
  jobs:
  - job: DeployJob
    displayName: AKS Deploy Job
    steps:

    # 🔁 Inject BuildId into manifest
    - script: |
        sed -i "s|__TAG__|$(tag)|g" manifests/deployment.yaml
        echo "Updated deployment.yaml:"
        cat manifests/deployment.yaml
      displayName: Replace image tag in manifest

    # 🚀 Apply manifests
    - task: Kubernetes@1
      displayName: Apply Kubernetes Manifests
      inputs:
        connectionType: 'Kubernetes Service Connection'
        kubernetesServiceEndpoint: 'aks-k8s-connection'
        namespace: 'default'
        command: 'apply'
        useConfigurationFile: true
        configuration: 'manifests/'

    # ✅ Verify rollout
    - script: |
        kubectl rollout status deployment joke-app -n default
      displayName: Verify Deployment
